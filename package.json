{
  "name": "glassmorphism",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "description": "Demo site",
  "main": "index.js",
  "author": "Mindaugas Kriksciukas",
  "license": "UNLICENSED",
  "scripts": {
    "start": "lerna run start --parallel",
    "build": "lerna run build --parallel",
    "lint:fix": "lerna run lint:fix --parallel",
    "lint:check": "lerna run lint:check --parallel",
    "ts:check": "lerna run ts:check --parallel"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
